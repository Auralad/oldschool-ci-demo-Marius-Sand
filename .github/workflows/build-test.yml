name: Container starten & testen 


on:
  push:
    branches: 
    - '**'


jobs:
  build_and_test:
    runs-on: ubuntu-latest
   
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Docker Version prüfen
        run: docker --version

      - name: oldschool-demo-image-Marius-Sand
        run: docker build -t oldschool-demo-image-marius-sand .        

      - name: Container starten
        run: docker run oldschool-demo-image-marius-sand
